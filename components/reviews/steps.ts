/**
 * The review flow, as the reviewer sees it. Shared by the form page, the
 * confirmation page and the thanks page so all three name the steps alike.
 */
export const REVIEW_STEPS = ["Write your review", "Confirm your email", "Published"] as const;
