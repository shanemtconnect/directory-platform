/**
 * The password-reset flow, as the person sees it. Shared by the ask page and
 * the set page so both name the steps alike.
 */
export const RESET_STEPS = ["Ask for a link", "Open the email", "Set a new password"] as const;
