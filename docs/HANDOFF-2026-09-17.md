# Handoff — directory-platform (2026-09-17, wave 2 shipped to staging)

Supersedes `docs/HANDOFF-2026-09-08.md` for state; that file's sections 3–7
(local setup, deploy, rules, architecture decisions, layout) still apply.

## 1. What landed since 2026-09-08

Branch `review-fixes` @ `2834528` (pushed; PR #1 → `main` still open) now
carries phases 3–7 on top of the review-fix pass:

| Task | Feature |
|---|---|
| 16 | Admin console: submission queue, approve/reject with decision email, city intro editor |
| 17 | Claims (domain-email magic link + document upload to a private R2 bucket), owner dashboard and listing editor |
| 18 | Public report / removal-request forms, suppression list, five-working-day SLA |
| 19 | Auto-city on submission (geocoder), location switcher |
| 20 | Ops: `/api/health`, Sentry, Plausible, worker heartbeat + uptime push |
| 21 | PayPal billing: plans, coupons, checkout with listing picker, webhook (fail-closed, idempotent), renewal reminders, hourly subscription sync |
| 22 | Reviews: submit → email-verified → published/held, owner replies, listing `/reviews` page, JSON-LD only when rendered |
| 23 | Verified badge with tracked snippet, backlink checker (SSRF-guarded, DNS-pinned), outreach batches + coupons |
| 24 | Stats: browser beacon → Redis → daily rollup; owner ROI panel |
| 25 | Admin report / removal queues |
| 26 | Auth polish: `?next=`, profile settings, password change/reset, email verification |
| 27 | CI + Coolify deploy workflow, smoke test, `.dockerignore` fix, `TestDb` moved out of `test/` |
| 28 | Rate-limit fix (in-flight Redis connect no longer stalls callers) |
| 29–30 | Security pass: beacon dedupe + per-IP/day guard, webhook body cap + throttle, magic tokens hashed at rest, job payload scrub + purge, review-confirm moderation guard, DNS-pinned backlink fetch |
| 31–32 | Admin claims/reviews queues reachable, ROI panel mounted, worker → ISR revalidation route, audit helper unification |
| 33 | README / CLONING env-var reference, scripts, worker jobs, operating guide |

Reviews and reports for every task: `.superpowers/sdd/task-N-{brief,report,review}.md`,
`final-review-wave2.md`, `security-review-wave2*.md` (gitignored, local).

## 2. Gates on `2834528`

| Check | Result |
|---|---|
| `pnpm typecheck` | clean |
| `pnpm test` (vitest, `--no-file-parallelism`) | 2326 / 2326 |
| `pnpm check:strings` | clean |
| `pnpm build:flags-off` / `build:flags-on` | both pass |
| `pnpm test:e2e` (Playwright) | 94 passed, 1 skipped by design (reviews flag-off) |
| `bash scripts/verify-image.sh` | PASS (runner + worker images; 9 migrations) |

## 3. Staging

https://directory.nowagogo.com — Coolify web `y00nhjwyfdkprqtbyp4bwdgm` (target `runner`,
branch `review-fixes`, `MIGRATE_ON_BOOT=true`, `SITE_ENV=staging`) and worker
`we5yexws4gxeldrhwhfdueoh`. Env added this wave: `INTERNAL_REVALIDATE_SECRET` on both
apps (value in `~/Claude/coolify-migration/.secrets/directory-staging.env`). Not set on
staging, by design: `PAYPAL_*` (billing switches itself off — pricing links to a
"no plan" page), `RESEND_*` (mail is logged, not sent), `R2_*` (claim uploads and
derivatives skip), `SENTRY_DSN`, `UPTIME_PUSH_URL`.

Live verification: see §7 (filled after the deploy).

## 4. Things a clone now needs (see docs/CLONING.md §3b, §4b)

PayPal app + `scripts/paypal-setup.ts` for plan ids; Resend domain; R2 buckets
(media public, claim-docs private, CORS rule); Turnstile keys;
`INTERNAL_REVALIDATE_SECRET`; promote the first admin with the SQL in README
"Operating the site".

## 5. Open items — Shane's decisions

1. Master plan in public git history (commit `aa903c2`) — `git filter-repo` or make the repo private.
2. Footer link matrix (~360 links/page) — cap or `/browse` hub.
3. `legalEntity: "TBC"`, `supportEmail: hello@example.co.uk` in `config/site.config.ts` (production build refuses them).
4. Delete the other session's 8 unhealthy Coolify DB/Redis resources (`directory-staging-db/redis`).
5. `.env.example` lines to add by hand (a permission rule blocks the automated session):
   `PAYPAL_ENV=`, `PAYPAL_PLAN_ESSENTIAL_MONTHLY=`, `PAYPAL_PLAN_ESSENTIAL_ANNUAL=`,
   `PAYPAL_PLAN_PREMIUM_MONTHLY=`, `PAYPAL_PLAN_PREMIUM_ANNUAL=`, `INTERNAL_REVALIDATE_SECRET=`,
   `SENTRY_DSN=`, `NEXT_PUBLIC_SENTRY_DSN=`, `NEXT_PUBLIC_PLAUSIBLE_DOMAIN=`, `UPTIME_PUSH_URL=`,
   `MIGRATE_ON_BOOT=`, `STATIC_ASSETS_DIR=`, `CACHE_NAMESPACE=`, `WORKER_ENABLED=`.
6. Merge PR #1 to `main`, then point both Coolify apps at `main`.
7. PayPal sandbox check still owed: create-time price override on a priced TRIAL cycle, and a `0.00` override for a 100 % coupon (`lib/billing/plans.ts`).

## 6. Wave D backlog (safe to pick up, none blocking)

- Backlink checker: constrain URL to the owner's `listings.website` domain, length cap; `worker/jobs/backlink-check.ts` `fetchImpl` type should be `BacklinkFetch`.
- `moderateReview` lacks an `email_verified_at` guard (queue UI cannot reach it, SQL could).
- Internal revalidate route: add a rate limit and a body cap.
- Beacon: `stats:seen` key stores a raw IP for 24 h — hash it; prefer `CF-Connecting-IP` when a CDN is in front.
- Extract a shared Redis client from `lib/spam/rate-limit.ts`, `lib/badge/counters.ts`, `lib/stats/redis.ts` (the latter two drop counts while a connect is in flight).
- Removal-request reject has no reason column (`lib/db/queries/trust.ts`); "five-working-day" copy hardcoded vs `REMOVAL_SLA_WORKING_DAYS`.
- Two direct `audit_log` inserts remain in `lib/db/queries/reviews.ts`; paginated city pages (`/page/N`) are not revalidated on status changes.
- `/search` impressions beacon; `listings.view_count` is never written; no retention policy on `listing_stats_daily`.
- `app/advertise/badge/page.tsx` declares `revalidate` but reads `searchParams` (dynamic).
- GitHub Actions pinned to major tags, not SHAs.
- Deploy note: raw magic/reset/review tokens issued before this deploy no longer resolve (they are now compared as digests); people re-request.

## 7. Live verification (2026-09-17, ~19:45 UK)

Deployed `review-fixes` @ `77350ce` (web deployment `wbfiyxt9…`, worker `bl7xouer…`).

| Check | Result |
|---|---|
| `/api/health` | `{"ok":true,"db":"ok","redis":"ok","build":"mfkYTiqnd6OGQejzTPJwA"}` |
| Web boot log | migrations "already up to date (9 applied)" (0008 applied on the first deploy), cache prefix `nextjs:<build>:`, no errors |
| Home, city, listing, pricing, login, signup, forgot-password, advertise/badge | 200, `X-Robots-Tag: noindex` (staging) |
| Listing page | renders `claim-link`, `/report/<id>`, `/remove/<id>`, stats beacon id |
| `/account`, `/admin`, `/admin/reviews`, `/checkout/…` anonymous | 307 → `/login?next=…` |
| Bogus ids on `/leave-review`, `/report`, `/badge` | 404 |
| `/api/internal/revalidate` GET | 405 (POST-only, secret-gated) |
| Worker | `running:healthy`; boot log lists all 10 jobs (derivatives, notify, heartbeat, flush-stats, purge-claim-docs, backlink-check, badge-counters, renewal-reminders, subscription-sync, purge-jobs) |

Two things learned the hard way, now fixed in the repo:
- `HEALTHCHECK NONE` on the worker stage made every Coolify worker deploy fail
  (Coolify waits for Docker "healthy"); the worker now has a liveness-file check.
- A Next layout's `redirect()` does not stop its page rendering; `/account` needed
  its own anonymous guard.
- Coolify 4.3.21's API returns empty `logs` for every deployment; diagnose from
  the container logs endpoint or the UI.

## 8. Wave D (2026-09-18) — backlog shipped

Plan: `docs/superpowers/plans/2026-09-18-wave-d.md`. `review-fixes` @ `cfd0abf`.

| Task | What landed |
|---|---|
| 34 | Owners can register where they put the badge (`/advertise/badge/mine?id=`), URL constrained to their own website domain, 10/hour per account; the public badge page is static again |
| 35 | One shared Redis client (`lib/redis/client.ts`) with a bounded in-process buffer: counts are held during a Redis blip, never a stall; writes that fail on a live socket are dropped, never doubled |
| 36 | Removal reject needs a reason (migration 0009), which reaches the audit row and the requester's email; SLA copy from one constant; paginated city pages and `/reviews` are revalidated with the listing |
| 37 | `/search` results count impressions; `listings.view_count` is the lifetime total; daily stats purged after `siteConfig.stats.retentionDays`; the one-view-per-day mark hashes the address; `CF-Connecting-IP` honoured only with `TRUST_CF_CONNECTING_IP=true` |
| 38 | Held reviews cannot be published unverified; claim document keys validated; presigned downloads are attachments; internal revalidate route rate-limited and body-capped; GitHub Actions pinned to SHAs |

Gates on `cfd0abf`: typecheck, strings, unit 2467, e2e 98 (+1 by-design skip), both flag builds, `verify-image.sh` (10 migrations) — all green.

New env (both optional): `TRUST_CF_CONNECTING_IP` (only behind Cloudflare), `STATS_SEEN_SALT` (defaults to `BETTER_AUTH_SECRET`). Add them to `.env.example` with the others in §5.

Left for later (all small): five callers still hand-name revalidate paths instead of `lib/revalidate/listing.ts`; `redisState()` could be surfaced in `/api/health`; `numberWord` covers 1–10.

Live verification: see §9.

## 9. Wave D live verification (2026-09-18, ~16:20 UK)

Deployed `review-fixes` @ `4e83e9c` (web `hkfkhp5v…`, worker `ccm564k9…`).

| Check | Result |
|---|---|
| `/api/health` | `{"ok":true,"db":"ok","redis":"ok","build":"96XnvcftF6v5rtF7wm0sF"}` |
| Web boot log | `0009_removal_rejection_reason` applied at boot (10 in total), cache prefix per build, no errors |
| `/advertise/badge` | 200, ISR (`x-nextjs-cache` present) — static again |
| `/advertise/badge/mine`, `/admin/reviews` anonymous | 307 → `/login?next=…` |
| `/search?q=…`, listing page | 200 |
| Worker | `running:healthy`; boot log lists all 11 jobs including `purge-stats` |
