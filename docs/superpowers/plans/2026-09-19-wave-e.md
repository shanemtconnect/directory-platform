# Wave E — leftovers, clone proof, audits, polish (2026-09-19)

Base: `wave-int` @ `cbdb7d1` (= `review-fixes`). Four independent tasks, each in its own
worktree, each reviewed before merge. Constraints: `.superpowers/sdd/global-constraints*.md`.

| Task | Scope |
|---|---|
| 39 | Leftovers: every revalidate caller through `lib/revalidate/listing.ts`; `redisState()` in `/api/health`; `numberWord` to 0–99 |
| 40 | Clone proof: `pnpm new-site --answers` end to end for a second niche (generate → seed → typecheck → build → e2e), fix what breaks, script it as `scripts/verify-clone.sh` |
| 41 | Lighthouse + axe audit of every page type on a production build; fix regressions; add a CI-runnable `scripts/audit-pages.mjs` with thresholds |
| 42 | UX polish of wave-2 screens (account, admin queues, claim/review/billing flows): consistent components, empty/error/loading states, mobile layout — no copy or feature removal |

Merge order: 39 → 41 → 42 → 40 (40 may touch config/seed writers; last so it validates the final tree).
