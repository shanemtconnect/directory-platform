# Wave C — 2026-09-12 (branches from `wave-int`, which already holds Tasks 16, 18, 19, 20)

Constraints: as `2026-09-12-phase-3-4-wave-a.md` (21–28) and the originals. Ports 3216–3218; Redis db 8, 9, 12.
Do not touch: `app/claim/**`, `app/account/page.tsx`, `app/account/listings/**` (Task 17), `lib/billing/**`,
`app/checkout/**`, `app/account/billing/**` (Task 21), `lib/routing/resolve.ts`, `app/[...segments]/page.tsx`,
`components/reviews/**` (Task 22), `lib/outreach/**`, `worker/jobs/backlink-check.ts` (Task 23), `lib/stats/**`,
`app/api/beacon/**` (Task 24). `worker/index.ts`, `lib/email/notify.ts`, `worker/jobs/notify.ts` are append-only.

---

## Task 25: Admin queues for reports and removal requests

**Owns:** `app/admin/reports/**`, `app/admin/removals/**` (new), `components/admin/ReportQueue.tsx`,
`components/admin/RemovalQueue.tsx` (new), `lib/actions/admin-trust.ts` (new), `components/admin/QueueCounts.tsx`
and `components/admin/AdminNav.tsx` **only** to link the two new queues, `e2e/admin-trust.spec.ts`.
Consume Task 18's exports in `lib/db/queries/trust.ts`: `listOpenReports`, `listOpenRemovalRequests`,
`actionReport`, `actionRemovalRequest` (both take `{ ip }`); use `writeAudit` from `lib/db/queries/audit.ts`
only if the trust module does not already audit (read it — it does; do not double-audit).

**Requirements:**
1. `/admin/reports`: open reports newest first (listing name + link, reason, detail, reporter email,
   age); actions **Dismiss** and **Mark actioned** (`actionReport`), each via a server action that
   `requireAdmin()`s and passes the request ip; optional "open listing in editor" link to
   `/admin/submissions/<id>` if that detail route accepts published listings (read Task 16's page;
   if not, link the public listing page).
2. `/admin/removals`: open requests ordered by `due_at` ascending with an **overdue** marker when
   `due_at < now()`; actions **Action** (removes the listing + suppression + requester email — all
   inside `actionRemovalRequest`) and **Reject** (reason). Show relationship, reason, requester.
3. Dashboard tiles for reports/removals now link to the queues; nav entries added.
4. e2e: create a report and a removal via the public forms (Task 18's routes exist on this branch),
   promote a user to admin (pattern in `e2e/admin.spec.ts`), action both, assert the listing is gone
   from its city page and a suppression row exists.

**Tests:** action-level tests (admin gate, ip recorded, outcomes surfaced), page self-gating.

---

## Task 26: Auth polish — `?next=`, password reset, email verification, account settings

**Owns:** `components/auth/**`, `app/login/**`, `app/signup/**`, `app/forgot-password/**`,
`app/reset-password/**`, `app/verify-email/**` (new), `app/account/settings/**` (new subfolder only),
`lib/auth/server.ts` (Better Auth config: email verification + password reset senders), `lib/auth/next.ts`
(new: safe `next` validation), `lib/db/queries/profile.ts` (new: read/update own profile),
`lib/actions/profile.ts` (new), `lib/email/templates/auth*.ts` (new), `e2e/auth.spec.ts`.

**Requirements:**
1. `/login?next=/claim/abc` returns to `next` after sign-in; `next` validated as a same-origin
   relative path (`/…`, no `//`, no scheme, no `\`), else `/account`. Same for signup.
2. Password reset: `/forgot-password` (rate-limited, always says "if that address exists…"),
   Better Auth `sendResetPassword` wired to a Resend template through the existing job queue
   (`notify.auth-reset` kind — append), `/reset-password?token=`. Email verification on signup:
   `sendVerificationEmail` → `notify.auth-verify` kind; `/verify-email` landing; unverified users can
   still sign in but see a banner on `/account` (do not block claims on it — the claim ladder has its
   own proof).
3. `/account/settings`: name, phone, marketing opt-in (profiles columns), change password (Better
   Auth), delete account request (creates a `removal_requests`-style row? No — out of scope; link
   `mailto:` support). All writes audited with ip via `writeAudit`.
4. Ensure a `profiles` row exists after signup (call `ensureProfile` in the post-signup path or rely
   on the lazy creation — verify which and test).

**Tests:** `next` validation, profile update gating (a user cannot edit another's profile), reset
token flow with a mocked mailer (read the token from the DB in e2e), verification banner logic.

---

## Task 27: CI and deploy automation

**Owns:** `.github/workflows/**`, `scripts/deploy-coolify.sh` (new), `scripts/smoke.sh` (new),
`docs/CLONING.md` and `README.md` **only** the CI/deploy sections, `.dockerignore` if needed.

**Requirements:**
1. `ci.yml`: unit job unchanged; e2e job creates and seeds `directory_e2e` via `scripts/e2e-db.sh`
   (Task 19 added it) instead of its own inline steps; matrix over both flag builds stays; Playwright
   browser cache; `docker-build` job builds BOTH `--target runner` and `--target worker`.
2. `deploy.yml`: on push to `main` (and manual `workflow_dispatch` with a branch input), after CI
   passes (`workflow_run` or `needs`), call the Coolify deploy API for the web and worker apps using
   repository secrets `COOLIFY_BASE`, `COOLIFY_TOKEN`, `COOLIFY_WEB_UUID`, `COOLIFY_WORKER_UUID`
   (`scripts/deploy-coolify.sh` does the POST + polls the deployment to `finished`/`failed`), then
   runs `scripts/smoke.sh <site url>`: `/api/health` 200 with `db: ok`, `/` 200, `/sitemap.xml` 200,
   a listing page 200; fails the job otherwise.
3. Document the secrets and the flow in README + CLONING (which uuids/where to find them; never commit
   tokens). No secrets in the repo (grep to prove it).
4. Sentry release marking is optional: if `SENTRY_AUTH_TOKEN` secret exists, upload source maps via
   `@sentry/cli` in the build job; otherwise skip silently (the pnpm `allowBuilds` for `@sentry/cli`
   is `false` — check whether the CLI can still run without its postinstall; if not, document why
   release upload is off).

**Tests:** shell scripts under `bash -n` and a `scripts/deploy-coolify.test.sh`-style dry run with a
fake `curl` (`CURL=./test/fake-curl.sh`); `actionlint` not available — validate YAML with `node -e`
using `js-yaml` if present in node_modules, else `python3 -c "import yaml"`.
