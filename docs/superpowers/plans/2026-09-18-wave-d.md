# Wave D — backlog from the 2026-09-17 ship (2026-09-18)

Base: `wave-int` @ `c0b7820` (= `review-fixes`). Five independent tasks, each in its own
worktree, each reviewed before merge. Constraints: `.superpowers/sdd/global-constraints*.md`.

| Task | Scope |
|---|---|
| 34 | Backlink registration UI + URL constraints; badge page ISR split |
| 35 | Shared Redis client (`lib/redis/client.ts`) with in-flight buffering for counters |
| 36 | Trust follow-ups: removal reject reason, SLA copy constant, review audit rows via `writeAudit`, paginated-city + reviews-path revalidation |
| 37 | Stats follow-ups: `/search` impressions beacon, `listings.view_count` written by flush, `listing_stats_daily` retention, hashed beacon seen-key, CF-Connecting-IP |
| 38 | Security minors: `moderateReview` verified guard, claim document key validation + attachment disposition, revalidate route limits, backlink `fetchImpl` type, GitHub Actions pinned to SHAs |

Merge order: 38 → 36 → 37 → 35 → 34 (35 touches the three Redis call sites 37 also edits —
37 keeps to `lib/stats` semantics, 35 to the client; expect a keep-both merge in
`lib/stats/redis.ts`).
