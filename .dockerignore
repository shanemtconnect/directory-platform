node_modules
.next
.next-pristine
.git
.github

# `.env` and `.env.local` alone let `.env.production` be baked into the image.
# Only the placeholder template belongs in there.
.env*
!.env.example

.superpowers
docs
e2e
test
test-results
playwright-report
seeds/**/*.md
*.log
coverage

# The worker stage COPYs the whole scripts/ directory (it needs seed-cli.ts
# etc. at runtime) — but not its own dry-run test harnesses.
scripts/*.test.sh
