node_modules/
.next/
.next-pristine/
# Next `next export` output — root only, so app/out/ (the sponsor click route) is tracked.
/out/
build/
.env
.env.local
*.log
.DS_Store
coverage/
drizzle/meta/_journal.json.bak
*.tsbuildinfo

# Commercial strategy — kept out of the public repo
docs/superpowers/plans/*master*.md

# Playwright
test-results/
playwright-report/

# Agent worktrees — never part of the repo
.claude/worktrees/
# Local SDD scratch reports — never intended for the public repo
.superpowers/

# Lighthouse + axe audit output (corepack pnpm audit:pages)
audit-report.json
